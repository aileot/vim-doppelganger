Describe 00-Prerequisite -- 10-Built-In:

  It requires Neovim, not Vim
    Assert True(has('nvim'))
    Assert Match($THEMIS_VIM, 'nvim')
  End

  It requires a function to set virtualtext
    Assert True(exists('*nvim_buf_set_virtual_text'))
  End

End

